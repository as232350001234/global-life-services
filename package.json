{
  "name": "global-life-services",
  "version": "1.0.0",
  "private": true,
  "description": "Global Life Services Navigator - Vue2 static site with bilingual UI, search and filters.",
  "scripts": {
    "flags:download": "node scripts/download-flags.mjs",
    "build": "rm -rf dist && mkdir -p dist/assets && cp index.html dist/ && cp -r assets/* dist/assets/ && cp robots.txt sitemap.xml dist/",
    "dev": "npx serve -l 5000 .",
    "check:links": "node scripts/check-links.mjs"
  }
}
